version: '3.9'

services:
  jenkins:
    build:
      context: ./jenkins
    container_name: etl-jenkins
    ports:
      - "8080:8080"
      - "50000:50000"
    environment:
      - REPO_URL=https://github.com/valdepeace/test-etl.git
      - BRANCH=main
      - LOCAL_REPO=./etl-repo
    volumes:
      - jenkins_home:/var/jenkins_home
      - etl_repo:/opt/etl-repo
      # Opcional: montar tu repo local por comodidad
      - /mnt/e/workspace/trustme/sumapositiva/pfs/poc-jenkins-etl/etl/etl-repo:/workspace/etl-repo
volumes:
  jenkins_home:
  etl_repo:
